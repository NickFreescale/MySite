# 个人信息配置说明

本文档说明了网站中个人信息的配置位置和修改方法。

## 📋 核心配置文件

### 1. `src/config/site.ts`

这是**最重要的配置文件**，大部分组件都从这里读取数据：

```typescript
export const siteConfig = {
  // 个人信息
  name: "Bill", 
  title: "图形学算法工程师", 
  description: "自中学起使用C++，聚焦算法与系统设计，覆盖图形学、控制算法与嵌入式等应用场景。", 
  location: "中国", 
  
  // 联系方式
  email: "19854814168@163.com",      // 你的邮箱
  linkedin: "",                       // LinkedIn链接（空字符串表示不显示）
  wechat: "19854814168",             // 微信号
  
  // 其他配置...
}
```

**修改这个文件后，以下组件会自动更新：**
- Footer（页脚）
- QuickContact（首页快速联系）
- Hero（首页英雄区域）
- 所有使用 `siteConfig` 的地方

---

## 📧 联系信息显示位置

### 2. `src/components/home/ContactSection.tsx`

首页的"联系我"部分，显示邮箱和微信卡片：

```typescript
const contactMethods = [
  {
    icon: Mail,
    title: '邮箱联系',
    description: '商务合作或技术交流',
    contact: '19854814168@163.com',
    href: 'mailto:19854814168@163.com',
    color: 'from-blue-500 to-cyan-500'
  },
  {
    icon: MessageCircle,
    title: '微信',
    description: '扫码添加微信好友',
    contact: '19854814168',
    href: '#',
    color: 'from-green-500 to-emerald-500'
  }
]
```

**已移除：** GitHub 联系方式

### 3. `src/components/about/AboutContact.tsx`

"关于我"页面的联系部分，包含详细的联系方式和简历下载：

```typescript
const contactMethods = [
  {
    icon: Mail,
    title: '邮箱',
    value: '19854814168@163.com',
    href: 'mailto:19854814168@163.com',
    // ...
  },
  {
    icon: MessageCircle,
    title: '微信',
    value: '19854814168',
    href: '#',
    // ...
  }
]

// 简历下载链接
const quickLinks = [
  // ...
  {
    title: '下载简历',
    description: 'PDF格式简历',
    href: '/resume.pdf',           // ← 简历文件路径
    icon: Download,
    external: true
  }
]
```

**底部邮箱按钮：**
```typescript
<a href="mailto:19854814168@163.com">立即联系我</a>
```

**已移除：** GitHub 联系方式

---

## 📄 简历下载配置

### 简历文件位置

**当前位置：** `front/public/resume.pdf`

这个文件是从 `front/CV/苏家镖-图形学简历.pdf` 复制过来的。

### 如何更新简历

1. **方法一：直接替换文件**
   ```bash
   # 将新简历复制到 public 目录，覆盖原文件
   copy "新简历.pdf" "front\public\resume.pdf"
   ```

2. **方法二：使用不同的文件名**
   - 将新简历放到 `front/public/` 目录
   - 修改 `AboutContact.tsx` 中的 `href` 值指向新文件名
   - 例如：`href: '/my-new-resume.pdf'`

### 简历访问方式

- **网页链接：** `https://你的网站域名/resume.pdf`
- **用户点击"下载简历"按钮时会自动下载这个PDF文件**

---

## 👤 个人头像

### 头像文件位置

**当前位置：** `front/public/images/profile.jpg`

### 使用位置

在 `src/components/about/AboutHero.tsx` 中：

```tsx
<img 
  src="/images/profile.jpg" 
  alt="头像" 
  className="w-full h-full object-cover"
/>
```

### 如何更新头像

1. 将新照片放到 `front/public/images/` 目录
2. 命名为 `profile.jpg`（或其他名称）
3. 如果使用不同名称，需要更新 `AboutHero.tsx` 中的 `src` 路径

---

## 📊 统计信息

### 1. "关于我"页面统计（`AboutHero.tsx`）

```tsx
<div className="text-center">
  <div className="text-3xl md:text-4xl font-bold text-primary-600 mb-2">6+</div>
  <div className="text-gray-600">完成项目</div>
</div>
<div className="text-center">
  <div className="text-3xl md:text-4xl font-bold text-primary-600 mb-2">3+</div>
  <div className="text-gray-600">年开发经验</div>
</div>
<div className="text-center">
  <div className="text-3xl md:text-4xl font-bold text-primary-600 mb-2">3+</div>
  <div className="text-gray-600">竞赛获奖</div>
</div>
```

**已移除：** GitHub 提交统计

### 2. "项目作品"页面统计（`ProjectsHero.tsx`）

```typescript
const stats = [
  { label: "完成项目", value: "6+" },
  { label: "技术栈", value: "5+" },
  { label: "竞赛获奖", value: "3+" }
]
```

---

## 🚀 Git 提交和部署

### 提交简历文件

是的，你需要将简历文件一起提交到 Git 仓库：

```bash
cd front
git add public/resume.pdf
git add src/config/site.ts
git add src/components/about/AboutHero.tsx
git add src/components/about/AboutContact.tsx
git add src/components/home/ContactSection.tsx
git commit -m "更新个人信息和简历"
git push
```

### Vercel 自动部署

提交到 GitHub 后，Vercel 会自动：
1. 检测到新的提交
2. 触发构建流程
3. 部署新版本
4. 你的简历文件会被包含在部署中

**部署后，用户可以通过以下方式下载你的简历：**
- 访问网站的"关于我"页面
- 点击"下载简历"按钮
- 系统会下载 `resume.pdf` 文件

---

## ⚠️ 注意事项

1. **邮箱地址统一性**
   - `site.ts` 中的 `email` 是主配置
   - 如果修改，记得同时更新 `ContactSection.tsx` 和 `AboutContact.tsx`

2. **简历文件大小**
   - 建议简历文件大小不超过 5MB
   - 这样可以确保快速下载和 Vercel 部署顺利

3. **隐私保护**
   - 确保简历中的个人信息是你愿意公开的
   - 网站上的简历是公开可访问的

4. **LinkedIn 已移除**
   - 如需重新添加，在 `site.ts` 中填写 `linkedin` 链接即可
   - 相关组件已有条件判断，会自动显示

---

## 📝 快速修改清单

**要更新联系信息，需要修改以下文件：**

- [ ] `src/config/site.ts` - 主配置（最重要）
- [ ] `src/components/home/ContactSection.tsx` - 首页联系卡片
- [ ] `src/components/about/AboutContact.tsx` - 关于页联系信息和底部按钮

**要更新简历：**

- [ ] 将新简历复制到 `front/public/resume.pdf`
- [ ] 提交到 Git 并推送

**要更新头像：**

- [ ] 将新照片放到 `front/public/images/profile.jpg`
- [ ] 提交到 Git 并推送

---

## 🔍 其他使用 siteConfig 的组件

这些组件会自动读取 `site.ts` 中的配置：

- `src/components/layout/Footer.tsx` - 页脚联系方式
- `src/components/home/QuickContact.tsx` - 首页快速联系
- `src/components/home/Hero.tsx` - 首页英雄区域

修改 `site.ts` 后，这些组件会**自动更新**，无需手动修改。

---

如有其他配置需求，请查看对应组件的源代码或联系开发者。





